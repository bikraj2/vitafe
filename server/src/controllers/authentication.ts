export const register = async (req: Request, res: Response) => {
  try {
  } catch (error) {}
};

interface userRegistration {
  firstName: String;
  lastName: String;
  password: String;
  username: String;
  email: String;
}
